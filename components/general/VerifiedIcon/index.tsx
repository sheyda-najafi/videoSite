import ToolbarIcons from '@/icons/toolbar';
import React from 'react';

export default function VerifiedIcon({ width = '14px', height = '14px', className = '' }) {
  return <ToolbarIcons.verfiedIcon width={width} height={'14px'} className={`${className}`} />;
}
